import { ViewStyle } from 'react-native';

interface Styles {
    centerAll: ViewStyle;
}

export const Styles: Styles = {
    centerAll: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
};

// export const StylesTd = {
//     flex
// }